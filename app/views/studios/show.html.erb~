<p id="notice"><%= notice %></p>
<div class="page-header">
	<h1><%= @studio.name %></h1>
</div>
		
<%= image_tag "img_estudio_big.jpg" %>

<ul  class="row">
	<li class="tipo">Estudio de: <span><%= @studio.show_type %></span></li>
	<li><%= @studio.description %></li>
</ul>

<p>
  <b>Address:</b>

</p>

<ul class="span4">
				<li><h2>Endereço, Horários e Contato</h2></li>
				<li>Segunda a Sexta: 8h até 22h</li>
				<li>Fins de Semana e Feriados: 12h até 22h</li>
				<li><%= @studio.address %></li>
				<li>Tel.: 1122-3344 </li>
</ul>
<div class="mapa"><%= gmaps4rails(@json) %></div>

<% if @studio.user_id == session[:user_id] %>
  <%= link_to 'Edit', edit_studio_path(@studio) %> |
<% end %>

<% if session[:user_id] and User.find_by_id(session[:user_id]).password == "admin" %>
  <%= button_to "Aprovar", approve_studio_path(@studio), :method => :put %>
<% end %>
